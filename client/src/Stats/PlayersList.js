import React from 'react';


const PlayersList = ({team}) => {
    
    return (
        <div>
            {/* {team}
            <ul id="playerlist">
            {players.map(player => (
                <PlayerContainer
                key = {player.id}
                player = {player} />
            ))}
            </ul> */}
        </div>
    );
}

export default PlayersList;
